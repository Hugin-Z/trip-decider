# trip-decider · Plan v4（解冻版）

> 版本：v4.0 · 2026-08-02
> 状态：**解冻**。v3.0 的冻结状态于 2026-08-02 正式撤销。
> 撤销原因：`docs/audit/handover-baseline.md` 实测确认，v3 §4 冻结的五态证据模型在产品运行路径上一次都不出现（基线报告 §3.2），其冻结不变式（v3:72）在 `trip_read_model.py:231-238` 处被反向违反（基线报告 §3.4 实测）。一份既未实现、也无法机械检验的冻结契约不构成约束，继续冻结只会让偏差累积。
> 仓库名 trip-decider 为占位，Hugin 可改。

---

## 0. 版本沿革与逐节处置

### 0.1 v3 → v4 的三项模型变更

| # | 变更 | 权威文件 |
|---|---|---|
| 1 | 废除五态 `display_status`，改为 `support` / `freshness` 两条正交轴 | `docs/contracts/evidence-axes.md` |
| 2 | `verified` 不再是枚举值，改为 `support:sourced ∧ freshness:fresh` 的合取，由读取层计算，不落盘 | `docs/contracts/evidence-axes.md` §4 |
| 3 | 陈旧证据的刷新分层：影响可行性的字段自动重查，其余仅标注待确认 | `docs/contracts/freshness-policy.md` |

新增硬规则：**任何持久化文件中不得出现展示状态字段。展示状态一律读时计算。** 可机械核对形式见 `docs/contracts/invariants.md` I1。

### 0.2 v3 逐节处置

| v3 章节 | 处置 | 说明 |
|---|---|---|
| §1 产品定位 | **保留** | 两个核心能力与差异化主张不变。见本文件 §1 |
| §2 产品边界 | **保留** | 做 / 不做的划分不变。见本文件 §2 |
| §3 管线总览 A–H | **修订** | 阶段划分保留；`[D]` 的「五态模型」作废，改指两轴。见本文件 §3 |
| §3 架构红线 1（城市专属） | **已裁决拆分** | 裁决 1（2026-08-02）采纳拆分：城市专属逻辑保留为红线并成为 `invariants.md` I9；语言词表降级为非红线。见本文件 §3.1 |
| §3 架构红线 2（阶段间以文件传递） | **修订** | 在线路径不满足，且不打算满足。见本文件 §3.1 |
| §3 架构红线 3（模块 B 契约 v0 定死） | **作废** | 模块 B 已在 `01a273b` 被整体重写（`dynamic_discovery.py` 为新文件，+753 行），该红线已被事实推翻 |
| §4 证据模型（五态） | **作废** | 由 `docs/contracts/evidence-axes.md` 取代 |
| §4 冻结不变式（v3:72） | **修订保留** | 语义保留，表述改为可机械核对形式，成为 `invariants.md` I2 |
| §4 执行红线（v3:74） | **保留** | conflicting / unknown 不得静默进入硬约束判定。成为 `invariants.md` I7 |
| §4 观察指标（verified 实旅证伪次数） | **修订** | `verified` 现为读时计算的 token，指标改为「token 为 `verified` 的事实被现实证伪的次数」 |
| §5 约束模型 | **保留** | 三层结构与权力边界不变。见本文件 §5 |
| §6 数据源 | **修订** | 「预期证据状态」列改用两轴表述。见本文件 §6 |
| §7 工件契约 | **修订** | v3 的 7 个工件属于离线管线；在线路径的落盘契约另有定义。见本文件 §7 |
| §8 v0 时间线 | **作废** | 日期硬指标放弃 |
| §9 验收标准 | **作废并重写** | 改为可机械核对形式，不含日期。见本文件 §9 |
| §10 v1 方向 | **保留** | 见本文件 §10 |
| §11 开放问题 | **修订** | 见本文件 §11 |

---

## 1. 产品定位（保留自 v3）

**从模糊出游意图出发，选出现实可达的目的地，并排出一份真正走得通、能解释、能修改的行程。**

| 能力 | 一句话 | 执行期 |
|---|---|---|
| A. 目的地发现 | 给定出发地 / 天数 / 预算 / 偏好，在可达圈内筛出 2-3 个差异化的可行目的地方案（Plan-backed：每个推荐背后至少跑通一个粗粒度可行方案） | v1 实现，v0 只定接口 |
| B. 行程结构决策 | 目的地确定后，决定住哪个片区、哪天走哪条线、天内怎么排、时间够不够；放不进去的点明确淘汰并说明原因 | v0 主攻 |

差异化主张（对外一句话）：**别的行程工具在数据不足时照样一本正经，这个系统会诚实说「这块我没底」。**

技术主线延续：verification > generation。

**v4 补注**：这句主张现在有了可机械核对的落点——`invariants.md` I2（展示不得高于支持）与 I3（不确定必须携带用户行动指引）。在 v3 下它只是一句话；基线报告 §3.4 表明系统当时的实际行为与它相反。

---

## 2. 产品边界（保留自 v3）

| 做 | 不做（长期） | 不做（当前） |
|---|---|---|
| 目的地筛选与理由 | 订票 / 订房 / 比价 / 库存 / 余票 | 多用户 / 账号 / 分享链路 |
| 基地（住宿片区）建议，含住宿 POI 密度校验 | 餐厅品味推荐（体验类只引来源不背书） | 自创完整 benchmark |
| **住宿价格区间**（作为预算硬约束的输入，不做订房与比价） | 长篇攻略文本 | |
| 分天 / 天内排序 / 时间可行性 | 与 OTA 比推荐和预订闭环 | |
| 无解时的冲突解释（哪几个条件打架、建议放松哪个） | | |
| 字段级证据状态 + 风险声明 | | |
| 约束修改后局部重排 | | |

**v4 变更 1**：v3「不做（v0）」列中的「Web UI（修改走约束文件重跑）」已被事实推翻——仓库现有三个宿主面（MCP STDIO、本地 HTTP + Web UI、Codex CLI 桥，基线报告 §1.3）。该项移出边界表。宿主面数量的收敛是一个未决决策（基线报告 D3）。

**v4 变更 2（裁决 2，2026-08-02）**：v3 的「不做：订票 / 订酒店 / 比价 / 库存」与证据策略产生过一次冲突——`hotel_price` 被配为 `feasibility_critical`，看起来越过了边界。裁决澄清：**价格区间必须做，订房与比价不做**。没有价格区间就无法判定预算硬约束，而预算是 §5 三层约束中的用户硬约束之一。相应地，余票（`seat_availability`）属订票域，已从证据登记表中删除（`freshness-policy.md` §2.2 注 1）。

---

## 3. 管线总览（修订自 v3）

```
[A] 需求澄清与约束化          实现（输入解析 → 结构化 intent）
        ↓
[B] 可达目的地候选池           实现（高德实时 POI 检索 + 分组排序）
        ↓
[C] POI 候选池生成             实现（零起点检索 + 用户指定点，双入口）
        ↓
[D] 字段级证据采集与定级        重做（support / freshness 两轴）   ← v3 此处为「五态模型」，作废
        ↓
[E] 约束形式化 + 冲突检查       实现（三层约束；无解时输出冲突集）
        ↓
[F] 结构求解                  实现（基地选址 + 分天 + 天内排序）
        ↓
[G] 行程卡输出                实现（两条：MCP App 与静态 HTML）
        ↓
[H] 约束修改 → 局部重排        实现（结构化 revision → 新 PlanVersion）
```

### 3.1 架构红线

**红线 1：不得存在任何城市专属的代码或配置。**

状态：**已裁决拆分**（裁决 1，2026-08-02）。

- **「不得存在城市专属逻辑」保留为红线**，可机械扫描，白名单为 `tests/` 与 `fixtures/`。已落为 `invariants.md` I9，测试文件 `tests/test_invariant_i9_no_city_specific_logic.py`，预期 P5 转绿。
- **「中文词表 / 行政区后缀」降级为非红线**，不属于违规。

基线报告 §4 列出的违规，按裁决重新归类：

| 位置 | 内容 | 裁决后归类 |
|---|---|---|
| ~~`src/trip_decider/destination_catalog.json`~~ | 硬编码 28 个中国目的地 | **已处置（P5 轮 1）**：整档移到 `examples/destination_catalog.json`。判定未放宽——`src/` 下不得有地名这一条原样保留，只是这份数据不再在 `src/` 下。移动时实测它在 `src/` 里**零读取点**（唯一读者 `destination_discovery.py` 早已删除），因此没有读取路径要跟着改 |
| `PRODUCT.md:22,27` | 自述只有「武汉—婺源」一条链路已验证 | **不违规**（文档陈述现状，非 `src/` 代码）；但它记录的单链路依赖是 I9 转绿的实际障碍 |
| `guided_discovery.py:715` | 硬编码行政区后缀 `("自治州","地区","市","县","区")` | **不违规**（语言配置，已降级） |
| `guided_discovery.py:723-736` | 硬编码中文区域词表 | **不违规**（语言配置，已降级） |
| `travel_agent.py:55-61` | 硬编码中文意图标记 `("倾向","优先","大概想去","考虑")` | **不违规**（语言配置，已降级） |

降级理由（记录裁决依据）：产品当前只服务中文用户与中国境内行程，把语言相关的词表当作违规会迫使做一层没有需求的抽象。城市专属配置则不同——它直接决定候选池的边界，是能力 A（目的地发现）能否成立的前提。

**红线 2：阶段间以文件传递（fixture-first，可重跑）。**

状态：**修订**。在线路径阶段间靠内存传递与线程（`trip_application.py:786-847`），不满足该红线，且不打算改造为满足。修订后的表述：**阶段边界必须可离线重放**——即给定同一份证据快照，重跑必须产出逐字节相同的结构。这保留了 fixture-first 的实际价值（可复现），不强制文件传递这一实现形态。`invariants.md` I5 的「结构部分逐字节稳定」是它的一个分量。

**红线 3：模块 B 的数据契约在 v0 定死，v1 实现时不得重构下游。**

状态：**作废**。`01a273b` 一次提交新增 `dynamic_discovery.py` 753 行、`guided_discovery.py` 770 行并重写 `product_web.py` 3,277 行，模块 B 已被整体重写。该红线已被事实推翻，保留它只会掩盖这一事实。

---

## 4. 证据模型（作废，指向新契约）

v3 §4 的五态模型作废。当前权威定义分布在三份契约文件：

| 内容 | 文件 |
|---|---|
| `support` / `freshness` 两轴定义、合取规则、`next_action` 结构、旧模型映射 | `docs/contracts/evidence-axes.md` |
| 各 data_type 的容忍窗、复用上限、超窗档位、「影响可行性」判定准则、PlanVersion 性质 | `docs/contracts/freshness-policy.md` |
| 必须始终成立且必须被测试核对的性质 I1–I9 | `docs/contracts/invariants.md` |

**从 v3 保留下来的两条**：

- v3:72 的冻结不变式「任何事实的展示状态不得高于证据实际支持的状态」——语义保留，表述改为两个分量的精确相等，成为 `invariants.md` I2。改表述的原因：原句在只有一个枚举的模型里无法机械比较，因而无法写成测试。
- v3:74 的执行红线「unknown 或 conflicting 的事实不得静默成为硬约束决策依据」——原样保留，成为 `invariants.md` I7。

**观察指标（非不变式）**：token 为 `verified` 的事实被现实证伪的次数。目标趋零，允许发生，发生即修数据管线。

---

## 5. 约束模型（保留自 v3）

三层结构，借 ChinaTravel 的分类（饮食 / 住宿 / 交通 / 时间 / 空间 / 景点 六类作初始词表），不照搬其 DSL：

| 层 | 内容 | 判定 |
|---|---|---|
| 环境可行性 | 点存在、开放时间内、交通段存在、活动不重叠 | 布尔，且依赖字段带证据状态 |
| 用户硬约束 | 必去 / 排除 / 最晚结束 / 预算 / 体力上限 / 是否换酒店 | 布尔；无解时输出最小冲突集 + 放松建议 |
| 软偏好 | 少折返 / 节奏松弛 / 多古村 / 少换乘 | 打分排序，不参与否决 |

权力边界：推荐（LLM / 检索）只负责入候选池；**入选权在约束 + 证据状态 + 结构决策**。高热度但绕路的点不得因推荐得分直接进入行程。

分工：LLM 负责需求解析、检索词生成、非结构化抽取、理由表达；确定性程序负责距离、时间预算、冲突检查、分天排序；用户负责必去、放弃与最终拍板。

**v4 补注**：「依赖字段带证据状态」现在的准确含义是「带 `support`，且读取时计算出 token」。可行性判定点的完整清单见 `freshness-policy.md` §3.1，该清单是契约的一部分，新增判定点必须同步更新。

---

## 6. 数据源（修订自 v3）

| 需要 | 来源 | 预期 support | 预期 data_type |
|---|---|---|---|
| POI 坐标 / 类型 | 高德 Web 服务 API（POI 2.0） | `sourced` | `poi_coordinate` |
| 点对点时间矩阵 | 高德路径规划（驾车 / 公交 / 步行） | `estimated` | `route_duration` |
| 跨城铁路时刻与票价 | 中国铁路 12306 | `sourced` | `railway_schedule_fare` |
| 目的地画像 / 住宿片区 | 高德 Web 服务 API | `sourced` 或 `estimated` | `destination_profile` |
| 开放时间 / 建议停留 | 搜索补充 | `sourced` 或 `unknown` | `opening_hours`（`reserved`） |
| 乡镇班车 / 景区接驳 | 搜索（游记 / 公众号） | `sourced` / `conflicting` / `unknown` | 【待验证】未登记 |
| **住宿价格区间**（做，用于预算硬约束） | 未接入 | `unknown` | `hotel_price`（`planned`，P5） |
| **住宿可订状态**（不做，属订房域） | — | — | — |
| **余票**（不做，属订票域） | — | — | — |

**v4 变更**：v3 表中的 `verified` 全部改为 `sourced`——`verified` 不再是可以在采集时赋予的值，它是读取时的合取结果。「点对点时间矩阵」明确为 `estimated`：它是供应商的推算量，不是直接读出（判定依据见 `evidence-axes.md` §2.2）。这项重分类会改变现有可行性判定的输入，见 `evidence-axes.md` §6.4。

data_type 的权威登记表见 `freshness-policy.md` §2.2。无生产者项的去留已由裁决 2 关闭：`seat_availability` 删除，`hotel_price` 为 `planned`，`opening_hours` 与 `ticket_price` 为 `reserved`。

待验证项（原 v3:117 保留）：高德个人开发者配额与商用条款。

---

## 7. 工件契约（修订自 v3）

v3 §7 定义的 7 个工件（`request.yaml` / `constraints.yaml` / `candidates.json` / `evidence.json` / `plan.json` / `violations.json` / `trip-card.html`）**只存在于离线 artifact 管线**，该管线从产品入口不可达（基线报告 §2.5：14 个模块 13,347 行，占 `src/` 的 36.4%）。

在线路径的实际落盘产物（基线报告 §2.4 实测）：`run.json` / `session.json` / `events.jsonl` / `plan-version.json` / `plans/plan-NNNN.json` / `evidence.json` / `guided-evidence.json` / `action-loop.json`，另有 `runtime/evidence-cache/records.json`。

**两条管线保留哪一条为主干是一个未决决策**（基线报告 D2），本文件不代为裁决。在裁决前，以下约束对**两条管线同时生效**：

1. `invariants.md` I1（持久化文件中不得出现展示状态字段）适用于任何一条管线的落盘产物。
2. PlanVersion 的新性质（结构 + 事实引用，不含展示态）见 `freshness-policy.md` §4。
3. 落盘契约变更必须携带版本标记。基线报告 H1 记录了一次无版本、无迁移的格式变更，其后果是此前全部 run 的已安装计划变为不可读。同类事故不得再发生。

修改—重排循环：结构化 revision → 新 PlanVersion。重排原则不变：最小改变，未被要求改变的部分尽量保留。

---

## 8. （原 v0 时间线，作废）

v3 §8 的十天时间线作废。理由：日期硬指标在阶段闸门面前没有信息量——它既不能加速工作，也不能判断工作是否合格。取而代之的是 §12 的六阶段闸门，每个闸门是可机械核对的条件，不是日期。

---

## 9. 验收标准（重写）

全部判据必须可机械核对，不含日期。每条给出核对方式。

### 9.1 契约层

| # | 判据 | 核对方式 |
|---|---|---|
| A1 | `invariants.md` 中 I1–I9 每条都有对应测试文件，且全部通过 | 测试套件执行结果 |
| A2 | 持久化产物中展示状态字段出现次数 = 0 | I1 的扫描器 |
| A3 | 展示 token 的产生实现数 = 1 | I6 的扫描器 |
| A4 | 对外返回值中，token ≠ `verified` 的事实携带合法 `next_action` 的比例 = 100% | I3a |
| A5 | 同一 run 在两个跨越容忍窗的读取时刻下，结构部分逐字节相同且至少一处 freshness 分量不同 | I5 |

### 9.2 工程层

| # | 判据 | 核对方式 |
|---|---|---|
| A6 | 从产品入口不可达的 `src/` 行数占比 | 可达性脚本（基线值 36.4%，13,347 行）。目标值取决于基线报告 D2 的裁决，裁决后填入 |
| A7 | 每个对外暴露的 MCP tool 至少有 1 个断言其返回契约的端到端用例 | 用例计数（基线值：10 个 tool / 3 个用例） |
| A8 | 本仓库文档中每一个可机械核对的计数与实测一致 | 计数核对脚本（基线报告 §4 列出 5 处不一致） |
| A9 | 测试套件全绿，且 0 跳过 | 测试执行输出（基线值：261 通过 / 0 跳过） |

### 9.3 产品层

| # | 判据 | 核对方式 |
|---|---|---|
| A10 | 存在一次真实行程，其间系统给出的每条 `verified` token 均未被现实证伪 | 人工记录，格式见 §9.4 |
| A11 | 存在 ≥3 处系统判断被现实证实或证伪且记录在案 | 同上 |
| A12 | 至少发生 1 次真实的约束修改重排，且新 PlanVersion 可用 | 运行记录 |

**失败信号（保留自 v3:159）**：全程没用它，或用了但全程在推翻它 → 假设不成立，回头重想，不许修修补补。

### 9.4 fixture 采集模板（保留自 v3:161-167）

| 计划 | 实际 | 偏差归因 |
|---|---|---|
| 例：14:00 到江岭 | 15:10 到 | 数据错（班车间隔比估算长） |

归因只允许三类：**数据错 / 时间估错 / 人改主意**。前两类是 bug，第三类是重排功能的需求来源，不得混记。

**v4 补注**：「数据错」现在要进一步分到两轴上——是 support 判高了（本该 `estimated` 却标了 `sourced`），还是 freshness 判宽了（容忍窗设得太长）。两者的修法不同，混记会让 §9.3 的记录失去诊断价值。

---

## 10. v1 方向（保留自 v3）

- 能力 A 实现：可达圈生成（高铁 2h/4h 圈 + 到站后公共交通 1h/2h 圈）→ 目的地级粗可行性（门到门时间 / 换乘 / 住宿密度 / 预算区间）→ 输出 2-3 个差异化方案（定位 + 取舍，不输出神秘匹配分）。
- 能力 A 验证：只用**答案未知**的征集请求；记录对方最终选择、是否成行、事后评价。
- 交互升级判据：依据修改日志，确认高频修改动作后，才把「必须保留 / 删除 / 换天 / 降低强度」做成消费级交互。

**v4 补注**：基线报告 M5 实测确认，当前候选生成的排序键只有 POI 命中数与标签字典序（`dynamic_discovery.py:114-120`），没有任何差异化目标函数；`playable_time_seconds` 在同一 intent 下只反映往返车程差（`guided_discovery.py:502-513`）。「差异化」目前是一句未实现的承诺，能力 A 的实现必须正面解决它。

---

## 11. 开放问题

| # | 问题 | 状态 |
|---|---|---|
| 1 | 架构红线 1 的裁决（保留 / 拆分降级 / 删除） | **已决**（裁决 1，2026-08-02）：采纳拆分。城市专属逻辑保留为红线，成为 `invariants.md` I9，白名单 `tests/` 与 `fixtures/`；中文词表 / 行政区后缀降级为非红线 |
| 2 | 两条管线保留哪一条为主干 | **已决**（2026-08-02，P4 规格裁决 13.5）：**在线管线为主干，离线管线删除**。14 个不可达模块 13,347 行、11 个 schema、约 140 个用例一并删除。依据：不可达（基线 §2.5）、五态已被两轴取代、参照价值已在 P2 消费完毕 |
| 3 | 宿主面收敛到几个 | 未决，基线报告 D3 |
| 4 | `runtime/sessions/` 的 56 个历史 run 是资产还是包袱 | 未决，基线报告 D4 |
| 5 | 「证据内核独立编写，不复用其他项目」是否也排除本仓库已有的 `evidence_runtime.py` | **已决**（裁决 3，2026-08-02）：可作为参考阅读，**不复用、不保留**，P4 随其余不可达模块一并删除 |
| 6 | 4 个无生产者 data_type 的去留 | **已决**（裁决 2，2026-08-02）：见 §6 与 `freshness-policy.md` §2.2 |
| 7 | 各可行性判定点对 `support == estimated` 的接受与否 | **已决**（裁决 5，2026-08-02）：可以参与判定，但不得产出无条件可行结论——`estimated` 输入必须至少产生一个 conditional。作为 `invariants.md` I7 的扩展分支，不新开不变式 |
| 8 | 高德个人开发者配额与商用条款 | 保留自 v3，未验证 |
| 9 | 乡镇班车信息的可获得率——决定 conflicting / unknown 在真实行程中的占比 | 保留自 v3，未实测 |
| 10 | I8 反向规则的 `planned` 修订（P1 为解决裁决 2 与 I8 的冲突所做） | **待 Hugin 复核**，见 `freshness-policy.md` §2.1.1 |

---

## 12. 六阶段计划与闸门

每个闸门是可机械核对的条件。未过闸门不进入下一阶段。

### P0 契约（本阶段）

**产出**：`docs/contracts/evidence-axes.md`、`docs/contracts/freshness-policy.md`、`docs/contracts/invariants.md`、本文件。

**闸门**：

1. 四份文档全部就位。
2. `invariants.md` 中每条不变式都有：陈述、判定方法、测试文件名、债务编号。
3. 本阶段无源码改动——`git diff --stat` 中 `src/`、`tests/`、`scripts/`、`schemas/` 四个路径为空。

### P1 不变式测试先行（红）

**产出**：I1–I9 的测试文件；预期失败登记机制；两份清点文档。不写任何实现。

**闸门**：

1. **预期失败登记机制就位**：一个机械可解析的登记文件，加一个断言「实际失败集合 == 登记集合」的元测试。多出失败（回归）与少了失败（悄悄转绿未登记）都必须失败。该机制的优先级高于任何单条不变式测试——P1 到 P5 期间套件长期为红，没有它红色就失去信息量。
2. 除 I8 外每条不变式各至少 1 个失败用例。
3. 失败原因是断言失败，不是导入错误或语法错误——即测试确实执行到了断言。元测试对 error 零容忍。
4. I8 转绿（纯静态检查，不依赖实现改造）。
5. `evidence-axes.md` §7 的未决项 1、2 完成清点，产出 `docs/contracts/reason-code-inventory.md` 与 `docs/contracts/support-reclassification.md`。
6. 本阶段不修改 `src/`——`git diff --stat -- src/` 为空。

### P2 证据内核

**产出**：`support` 判定、`freshness` 计算、token 合取、`next_action` 构造的纯函数内核（`src/trip_decider/evidence_core.py`）。独立编写，不接入产品路径。

**闸门**：

1. 内核零 I/O：不读文件、不读时钟、不发网络请求。`now` 与策略表均由调用方注入。
2. 内核不 import 任何产品模块——依赖方向单向。
3. **I2 与 I3a 在内核范围转绿**（`tests/test_invariant_i2_kernel_*.py`、`tests/test_invariant_i3a_kernel_*.py`）。读取层范围的同名不变式重定向到 P3a。
4. 内核覆盖 `evidence-axes.md` §4.1 的全部 8 个 token、§2.4 的全部 4 条聚合分支、§2.2.1 的 `confirmed_absent`、§5.1 的双向约束、§5.2 每个枚举字段的域校验。
5. 登记文件已同步：`current_phase`、`phase_order` 与 `exemptions` 类目就位，元测试仍绿。

**2026-08-02 修订（A1）**：本闸门第 3 条原为「I2、I3a、I5、I6 在内核范围内转绿」，不成立。I6 的判定要扫描整个 `src/`，P2 阶段旧的 5 处映射实现仍在，不可能绿；I5 需要读真实 run 目录并跨两个读取时刻，超出纯函数内核范围。二者移至 P3。

### P3a 读取层接管（不做重分类）

**2026-08-02 裁决**：原 P3 拆为 P3a / P3b。理由：读取层接管与 support 重分类是两件独立的事，捆在一起会让一次改动同时改变「谁算 token」和「哪些证据算 sourced」，出问题时无法定位是哪一半造成的。

**产出**：把 token 计算收敛到 `evidence_core`，删除并行实现。**重分类不生效**——`support-reclassification.md` §1 的 3 处高德路径规划时长维持旧标签（仍标 `sourced`）。

**闸门**：

1. **I6 转绿**：白名单只有 `evidence_core` 与渲染层常量表；写入侧 7 个模块登记为豁免（`expires_at_phase = P4`）。
2. **I5 转绿**：读取层具备 `now` 注入点，该注入点在本阶段建立。
3. **I2 除豁免项外转绿**：`support == estimated` 在读取层不可达（持久化枚举无该值），登记为豁免，`expires_at_phase = P3b`。
4. **I3a 读取层两条转绿、I3b 转绿**。候选卡一条属写入侧，转 P3b。
5. **读取层**的废弃实现确实被删除，不是被绕过：`trip_read_model.py` 的 36 处展示态字面量清零。
6. 对外返回值的 diff 已逐项评审——产出 `docs/contracts/p3a-outward-diff.md`，每一处变化标注依据（不变式要求 / 顺带清理 / 其他）。
7. 登记文件已同步，元测试仍绿。

**2026-08-02 实测修正（P3a 执行时发现）**：原闸门 1 要求「全仓 token 实现数 = 1」，与本阶段「不碰持久化」的硬边界冲突。91 处字面量中只有 `trip_read_model.py` 的 36 处属读取层；其余 55 处分布在 7 个写入侧模块，它们的字面量被烘进 `run.json` / `plan-version.json` / `evidence-cache` 的字节内容，删除即改动落盘。该 55 处按豁免机制登记到 P4（I1 的范围）一并清理。

### P3b 闸门四态化（重分类生效）

**产出**：`support-reclassification.md` §5 列出的 **29 处 `sourced` 硬闸门**逐点改造为四态感知；重分类生效；豁免清零。

**前置条件（硬性）**：**P3b 开始前必须产出 29 个闸门的逐点改造清单，清单过 Hugin 后才动代码。** 每点写明：

| 列 | 内容 |
|---|---|
| 位置 | `文件:行号` |
| 现状语义 | 当前这个闸门在 `sourced` / 非 `sourced` 两种情况下各做什么 |
| 改造后语义 | 四态各自的处理，特别是 `estimated` 走哪一支 |
| 对规划结果的预期影响 | 该点改造后，候选 `feasibility_status` 或 `planning_state` 会如何变化 |

不先出清单就改代码，等于在 29 个决策点上逐个即兴发挥——基线报告记录的四套状态词表就是这么来的。

**清单已产出**：`docs/contracts/p3b-gate-inventory.md`（2026-08-02），**待 Hugin 确认**。四型分布：甲 20 / 乙 4 / 丙 4 / 丁 1。其中 3 点需裁决，见该文件 §8。

**P3b 前置修正（已完成，2026-08-02）**：P3a 问题 1（缓存降级值渲染为 `verified`）已裁决落地——`refresh_failure` 转正为契约字段，freshness 增加刷新失败封顶，`reason_code` 新增第 15 个 `refresh_failed`。见 `evidence-axes.md` §3.4、`invariants.md` I1 白名单与 I2 边界说明。

**闸门**：

1. **I2 无豁免全绿**：`invariant_ledger.json` 的 `exemptions` 为空数组。
2. **I7 转绿**，含裁决 5 的扩展分支——`estimated` 输入至少产生一个 conditional，有直接用例，不靠间接推断。
3. 29 个闸门逐点改造清单已过 Hugin，且每点在代码中可追溯。
4. 登记文件已同步，元测试仍绿。

### P4 持久化清理

**产出**：从落盘契约中移除展示态；PlanVersion 改为结构 + 事实引用。

**前置条件（硬性）**：**P4 开始前必须产出落盘契约 v2 规格，规格过 Hugin 后才动代码。** 与 P3b 的闸门清单同理——落盘契约一次改错的代价是全部历史数据不可读（基线报告 H1）。

**规格已产出**：`docs/contracts/persistence-v2.md`（2026-08-02），**待 Hugin 确认**。实测一次全新 run 落盘 476 处 I1 禁用字段；规格逐文件给出删除项、新增项与引用结构，并含 blocker 家族清理（21 → 11）、历史 run 处置、`DEFAULT_AGENT_STORE` 惰性化三件。其中 5 项需裁决，见该文件 §12。

**P3b 尾巴（已完成，2026-08-02）**：裁决 3 的 `schema_version` 打标补齐 `evidence/current.json` 与 `evidence/guided-comparison.json`。三份独立的原子写实现统一走 `travel_agent.stamp_schema_version()`。

**闸门**：

1. I1 转绿（含 I1 未决项 2：禁用键名集合已对全部历史 session 目录做过一次全量归纳）。
2. I5 转绿（含持久化侧）。
3. 落盘契约携带版本标记，且存在一个跨版本读取的用例。
4. 历史 run 的处置结论已落地（基线报告 D4 裁决后执行）。
5. PlanVersion 安装闸门已改为读时重算——`travel_agent.py:954-968` 与 `trip_query.py:311-318` 不再依赖持久化的 `planning_state` / `displayable`。

### P5 刷新分层

**产出**：`feasibility_critical` 的自动重查；其余标注待确认。

**闸门**：

1. I4、I7、I9 转绿。
2. `freshness-policy.md` §2.2 表中每个 data_type 的 `on_stale` 档位都有对应用例。
3. `freshness-policy.md` §3.1 的可行性判定点清单与代码实际一致——存在一个测试断言清单完备（新增判定点会导致该测试失败）。
4. §2.2 中 `status == planned` 的项已全部转为 `active` 或降为 `reserved`——本阶段目标为 `hotel_price`（生产者落地）。这是 I8 `planned` 态的补偿控制，见 `freshness-policy.md` §2.1.1。
5. `seat_availability` 已从 `evidence_broker.py` 与 `tests/test_evidence_broker.py` 移除。
6. §9.1 的 A1–A5 全部满足。
7. 登记文件为空——套件全绿，元测试断言空集。

---

## 13. 阅读顺序（面向新接手者）

1. `docs/audit/handover-baseline.md` — 接手时的实测状态，全部结论带 `文件:行号`。
2. 本文件 §0.2 — v3 的哪些部分还成立。
3. `docs/contracts/evidence-axes.md` — 证据模型的权威定义。
4. `docs/contracts/freshness-policy.md` — 新鲜度策略与 PlanVersion 性质。
5. `docs/contracts/invariants.md` — 必须始终成立的性质，以及它们各自的测试在哪。
6. `docs/contracts/engineering-discipline.md` — 作业纪律的单一出处，每条带一次真实事故。作业单按编号引用（「按 D5 处理」），不再逐条展开。

`PRODUCT.md` 与 `README.md` 的多处计数与实际不符（基线报告 §4 列出 5 处），在 §9.2 A8 转绿前不应作为核对依据。
