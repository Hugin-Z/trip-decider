# trip-decider · Plan v3(冻结版)

> 版本:v3.0 · 2026-07-26 冻结
> 状态:方案冻结,进入执行。除非 v0 验证推翻核心假设,不再修改产品定义。
> 仓库名 trip-decider 为占位,Hugin 可改。

---

## 1. 产品定位

**从模糊出游意图出发,选出现实可达的目的地,并排出一份真正走得通、能解释、能修改的行程。**

两个核心能力,产品定义第一天即完整,执行分期:

| 能力 | 一句话 | 执行期 |
|---|---|---|
| A. 目的地发现 | 给定出发地/天数/预算/偏好,在可达圈内筛出 2-3 个差异化的可行目的地方案(Plan-backed:每个推荐背后至少跑通一个粗粒度可行方案) | v1 实现,v0 只定接口 |
| B. 行程结构决策 | 目的地确定后,决定住哪个片区、哪天走哪条线、天内怎么排、时间够不够;放不进去的点明确淘汰并说明原因 | **v0 主攻** |

差异化主张(对外一句话):**别的行程工具在数据不足时照样一本正经,这个系统会诚实说"这块我没底"。**

技术主线延续:verification > generation。这是 evidence-runtime 方法论在 ToC 域的第二个实例。

## 2. 产品边界

| 做 | 不做(长期) | 不做(v0) |
|---|---|---|
| 目的地筛选与理由 | 订票 / 订酒店 / 比价 / 库存 | Web UI(修改走约束文件重跑) |
| 基地(住宿片区)建议,含住宿 POI 密度校验 | 餐厅品味推荐(体验类只引来源不背书) | 目的地发现的完整实现 |
| 分天 / 天内排序 / 时间可行性 | 长篇攻略文本 | 多用户 / 账号 / 分享链路 |
| 无解时的冲突解释(哪几个条件打架、建议放松哪个) | 与 OTA 比推荐和预订闭环 | 自创完整 benchmark |
| 字段级证据状态 + 风险声明 | | |
| 约束修改后局部重排 | | |

## 3. 管线总览

```
[A] 需求澄清与约束化          v0:实现(输入解析 → constraints.yaml)
        ↓
[B] 可达目的地候选池           v0:仅接口 + 直通(目的地已给定时 pass-through)
        ↓                     v1:可达圈生成 + 目的地级粗可行性
[C] POI 候选池生成             v0:实现(零起点检索 + 用户指定点,双入口)
        ↓
[D] 字段级证据采集与定级        v0:实现(五态模型)
        ↓
[E] 约束形式化 + 冲突检查       v0:实现(三层约束;无解时输出冲突集)
        ↓
[F] 结构求解                  v0:实现(基地选址 + 分天 + 天内排序,算法从简)
        ↓
[G] 行程卡输出                v0:实现(HTML 单文件)
        ↓
[H] 约束修改 → 局部重排        v0:实现(改 yaml 重跑;重排尽量保留未被要求改变的部分)
```

架构红线:
1. **不得存在任何城市专属的代码或配置**。城市差异只允许表现为运行时的证据状态差异。
2. 阶段间以文件传递(fixture-first,可重跑,与 tender-writer/solution-drafter 同款工程约定)。
3. 模块 B 的数据契约在 v0 定死,v1 实现时不得重构下游。

## 4. 证据模型(核心资产)

每个进入决策的事实字段,必须携带五态之一:

| 状态 | 含义 |
|---|---|
| verified | 当前、明确、可回读的权威来源支持 |
| sourced | 有来源,但权威性 / 时效性 / 独立验证不足 |
| estimated | 地图 API、算法或规则推算 |
| conflicting | 不同来源冲突 |
| unknown | 没有找到足够信息 |

**Invariant(冻结,不可协商):任何事实的展示状态不得高于证据实际支持的状态。**

执行红线:**unknown 或 conflicting 的事实不得静默成为硬约束决策依据**。正确形态是条件化输出,例:"若班车能在 8:30 前到达,该方案可执行;当前班次未核验,建议改包车/自驾,或由用户补充班次"。禁止为了产出完整行程而悄悄假设。

观察指标(非 invariant):verified 字段的实旅证伪次数。目标趋零,但允许发生,发生即修数据管线。

约束-证据依赖 schema(示意):

```yaml
constraint:
  id: arrive_before_sunset
  expression: arrival_time("江岭") <= sunset_time
dependencies:
  - field: travel_time      # status: estimated
  - field: road_status      # status: unknown
  - field: sunset_time      # status: verified
evaluation:
  result: conditionally_feasible
  reason: "道路状态未知,当前仅按地图驾车时长估算"
```

## 5. 约束模型

三层结构,借 ChinaTravel 的分类(饮食/住宿/交通/时间/空间/景点 六类作初始词表),不照搬其 DSL:

| 层 | 内容 | 判定 |
|---|---|---|
| 环境可行性 | 点存在、开放时间内、交通段存在、活动不重叠 | 布尔,且依赖字段带证据状态 |
| 用户硬约束 | 必去 / 排除 / 最晚结束 / 预算 / 体力上限 / 是否换酒店 | 布尔;无解时输出最小冲突集 + 放松建议 |
| 软偏好 | 少折返 / 节奏松弛 / 多古村 / 少换乘 | 打分排序,不参与否决 |

权力边界:推荐(LLM/检索)只负责入候选池;**入选权在约束 + 证据状态 + 结构决策**。高热度但绕路的点不得因推荐得分直接进入行程。

分工:LLM 负责需求解析、检索词生成、非结构化抽取、理由表达;确定性程序负责距离、时间预算、冲突检查、分天排序;用户负责必去、放弃与最终拍板。

## 6. 数据源(v0)

| 需要 | 来源 | 预期证据状态 |
|---|---|---|
| POI 坐标 / 类型 / 部分开放时间 | 高德 Web 服务 API(POI 2.0) | verified / sourced |
| 点对点时间矩阵 | 高德路径规划(驾车/公交/步行) | estimated |
| 开放时间 / 建议停留(缺失时) | LLM + web 搜索补 | sourced / unknown |
| 乡镇班车 / 景区接驳 | 搜索(游记/公众号) | sourced / conflicting / unknown |
| 住宿 | 只做片区级 POI 密度校验,不接房价 | estimated |

待验证项(D1 落地时确认并记入 docs):高德个人开发者配额与商用条款。

## 7. 工件契约

```
request.yaml        # 出发地/时间窗/人数/交通方式/体力/偏好;目的地(v0 必填,v1 可空)
constraints.yaml    # 用户可直接编辑:must_visit / excluded / day_constraints / preferences
      ↓  run
candidates.json     # 候选池(含来源)
evidence.json       # 字段级证据:值 + 状态 + 来源 + 采集时间
plan.json           # 结构化行程(活动级,含时间与交通段)
violations.json     # 未满足项 / 冲突集 / 条件化可行项
trip-card.html      # 单文件行程卡:分天时间轴 + 地图动线 + 交通/停留时间
                    #   + 缓冲区 + 证据状态标注 + 淘汰名单及原因 + 决策理由
```

修改-重排循环 = 改 constraints.yaml → 重跑 → 新 trip-card.html。重排原则:最小改变,未被要求改变的部分尽量保留。

## 8. v0 时间线(2026-07-27 → 08-05)

| 天 | 产出 | 备注 |
|---|---|---|
| D0(半天,硬性限时) | 一页对照表:现有方法已解决 / 可直接复用 / 真实旅行仍未解决 | 读 ChinaTravel(约束分类+DSL 思路+数据集是否可下载)、Hao(unsat core 与放松建议)、ItiNera(零起点候选池分阶段)。超时即停,不许延长 |
| D1-2 | 高德 key;POI 检索 + 时间矩阵跑通;婺源/上饶/三清山 fixture 落库;evidence.json 五态落地 | |
| D3-5 | 约束形式化 + 冲突检查 + 结构求解(基地/分天/排序/理由);无解解释 | 算法从简:枚举基地 × 贪心分天 + 天内局部优化,可解释优先 |
| D6-7 | trip-card.html + 证据状态标注 + 淘汰名单 | |
| D8-9 | 用真实 request 跑江西行程;人工对照修正;测约束修改重跑 | |
| D10 | 定稿,带着上路 | |

并行零成本动作(今天即可开始,不占开发时间):向朋友/读者征集"不知道去哪"的真实请求,存为 v1 的待验证 fixture。

## 9. 验收标准(预注册,行前冻结)

v0 验证的是能力 B(行程结构),不验证能力 A(目的地发现——本次目的地已定,答案已知,不构成有效验证)。

| 类型 | 判据 |
|---|---|
| 硬 | 8.5 前产出江西行程且实际按它走(至少部分) |
| 硬 | 全程零次"标称 verified、实际为错"(invariant 检验) |
| 软 | ≥3 处系统判断被现实证实或证伪且记录在案 |
| 软 | 旅行中至少发生 1 次真实的约束修改重跑,且结果可用 |
| 行为 | 是否主动再次打开;是否愿意直接发给同行者;同行者不听解释能否看懂 |
| **失败信号** | 全程没用它,或用了但全程在推翻它 → 假设不成立,回头重想,不许修修补补 |

fixture 采集模板(旅行中随手记):

| 计划 | 实际 | 偏差归因 |
|---|---|---|
| 例:14:00 到江岭 | 15:10 到 | 数据错(班车间隔比估算长) |

归因只允许三类:**数据错 / 时间估错 / 人改主意**。前两类是 bug,第三类是重排功能的需求来源,不得混记。

## 10. v1 方向(旅行后启动)

- 能力 A 实现:可达圈生成(高铁 2h/4h 圈 + 到站后公共交通 1h/2h 圈)→ 目的地级粗可行性(门到门时间/换乘/住宿密度/预算区间)→ 输出 2-3 个差异化方案(定位 + 取舍,不输出神秘匹配分)。
- 能力 A 验证:只用**答案未知**的征集请求;记录对方最终选择、是否成行、事后评价。
- 交互升级判据:依据 v0 修改日志,确认高频修改动作后,才把"必须保留/删除/换天/降低强度"做成消费级交互(小程序评估随之启动,含个体工商户与备案事项)。

## 11. 开放问题(待验证清单)

1. 高德个人开发者配额与商用条款(D1)
2. ChinaTravel 数据集与 DSL 是否可直接下载复用(D0)
3. Hao et al. 正式发表版的确切成绩口径(97% 见于 ChinaTravel 引用;另有 93.9% 一说未核实)(D0,不阻塞)
4. 乡镇班车信息的可获得率——决定 conflicting/unknown 在真实行程中的占比(D8-9 实测)