# trip-decider

## 产品定位

trip-decider 从模糊出游意图出发，先形成现实可验证的目的地候选，再在用户确定目的地后生成从出发到返程的完整行程。系统不得用模型猜测替代交通、价格、营业时间或可行性证据。

## 两阶段主线

1. **Discover**：模糊意图 → 候选种子排序 → 补齐跨城时间、预算和粗计划 → 现实可达的目的地候选。
2. **Plan**：目的地确定 → 跨城往返、住宿基地、当地地点、路线、每日时间轴、预算和备选方案。

`destination_catalog.json` 只是候选种子库。`destination_discovery.py` 默认输出 `PRELIMINARY_NOT_FEASIBILITY_VERIFIED`；只有接入真实交通评估的候选才升级为四态可行性。

`simple_live.py` 是 Plan 阶段的当地规划服务，不是产品入口。产品入口是 `product_web.py`；它负责 Discover/Plan 状态切换，并调用当地规划服务。

## 当前真实能力

- 28 个国内目的地候选种子及确定性初排；
- provider-neutral 高德行政区与 POI parser；
- `matched / ambiguous / unmatched`，Web 多候选必须由用户显式选择；
- GCJ-02 地点身份、公交优先的当地路线与粗时间轴；公交无方案时才比较驾车侧备选；
- 武汉到婺源区域的 12306 实时直达车次、二等座票价，以及婺源站/上饶站到婺源县代理住宿片区的高德接驳评估；
- 武汉—婺源3.5天纵向方案：县城/篁岭住宿片区比较、四日公交/景区班车时间轴、景点开放与门票来源、逐段上车信息和六类预算；
- Evidence 状态、constraints、coarse planner 和 HTML 转义；
- 两阶段本地 Web 页面骨架。

除武汉到婺源区域这一条已验证链路外，其他目的地初排仍没有使用真实跨城时间或交通价格；任何候选的完整预算也尚未验证。

## 当前缺失数据源

- 武汉—婺源区域以外的铁路，以及航班和公路的时刻、余票、票价与换乘；
- 住宿区域、酒店坐标、价格和可订状态；
- 景点开放时间、门票、临时关闭和建议停留依据；
- 天气预报以及雨天备选地点；
- 从跨城抵达时刻到当地路线的统一时间与费用模型。

## Legacy governance

以下历史内容保留作审计记录，但不属于产品运行路径：

- `plans/work-unit-*.md`（当前 21 个）；
- `docs/reviews/work-unit-*-review.md`（当前 18 个）；
- `scripts/verify_wu*.ps1`（当前 12 个）；
- `scripts/run_amap_ephemeral_live.ps1`（历史 live/smoke 入口）。

不得从 `product_web.py`、Discover 或 Plan 运行时调用这些文件。

## 当前纵向验收用例

**武汉出发，婺源三天半。**

当前已从真实跨城交通进入 Plan，并覆盖：

- 武汉到上饶/婺源的去程与返程；
- 抵达、返程时间和三天半可用窗口；
- 县城老北站/火车站公交走廊与篁岭住宿片区比较；2026-08-05至08-08具体酒店成交价和可订状态仍为 `unknown`；
- 当地地点身份、景点间路线和每日时间轴；
- 交通、住宿、门票与当地交通预算；
- 雨天和时间不足备选；
- 所有缺失或冲突事实显式展示，不能静默补值。
