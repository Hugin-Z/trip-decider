# Known Issues

本文件记录当前版本已经确认的限制、风险和可用规避方式。它们不是未来能力承诺；
状态变化应同时更新这里、`README.md` 和相关测试。

## 安全与部署

| 限制 | 影响 | 当前处置 |
| --- | --- | --- |
| Web 服务没有认证、TLS 或多租户隔离 | 非本机用户若能访问端口，可能读取或修改行程 | 保持默认 `127.0.0.1`；不要直接暴露到局域网或公网 |
| 凭据来自进程环境 | 终端历史、进程配置或截图仍可能泄露 key | 使用临时环境变量；不把真实值写入仓库、配置示例或截图 |
| runtime store 是单进程写入模型 | 两个进程共享同一 runtime root 会产生状态竞争 | owner lock 会拒绝重复启动；并行实例使用不同的 `TRIP_DECIDER_RUNTIME_ROOT` |

## 数据与产品覆盖

| 限制 | 影响 | 当前处置 |
| --- | --- | --- |
| 只有武汉到婺源区域完成端到端纵向实测 | 其他目的地可能缺跨城或当地数据，不能视为全国支持 | 把结果当预览；检查每个域的证据 token 与下一步动作 |
| 跨城只覆盖铁路主路径 | 航班、长途公路和复杂换乘尚未形成统一模型 | 无来源时保持未知；不要手工脑补为可行 |
| 酒店精确价格与可订状态没有稳定数据源 | 住宿预算可能只含片区或候选，不含可成交房价 | 出行前到预订渠道复核；总预算区分已知与未知 |
| 景点开放、门票和天气覆盖不完整 | 时间轴与预算可能保留待确认项，雨天方案可能不充分 | 依据输出中的 `next_action` 在出发前复核 |
| 目的地种子目录只是预筛输入 | 目录标签不是现实可达性或推荐质量证明 | 最终排序只使用实查可达性；可用 `TRIP_DECIDER_DESTINATION_POOL` 替换整份目录 |

## 外部服务

| 限制 | 影响 | 当前处置 |
| --- | --- | --- |
| 12306 的查询窗口、限流和网络状态会变化 | “查无实据”可能来自日期过远或临时失败，不等于车次不存在 | 重试、缩短日期跨度，或按系统声明的字段提交人工证据 |
| 高德 Web 服务需要有效 key 与可用配额 | 地点、POI 或当地路线会停在未采集 | 检查 `AMAP_WEB_SERVICE_KEY`、配额和网络；不要在 issue 中粘贴 key |
| 网页地图另需 JS key 与安全密钥 | 行程可计算但地图区域不能正常渲染 | 配置 `AMAP_JS_API_KEY` 与 `AMAP_JS_SECURITY_CODE`，或忽略地图视图 |

## 兼容性与开发体验

| 限制 | 影响 | 当前处置 |
| --- | --- | --- |
| 官方启动脚本只在 Windows PowerShell 验证 | macOS/Linux 没有等价的一键脚本 | 手动设置 `PYTHONPATH=src` 后运行 `python -m trip_decider.product_web` |
| Python 版本限定为 3.11 | 3.12+ 或更早版本不在当前测试矩阵 | 使用独立的 Python 3.11 虚拟环境 |
| runtime schema 不提供 v1→v2 迁移 | 旧运行数据不能安全复用 | 升级前备份；使用新 runtime root 或删除可再生成的旧数据 |
| 当前没有托管部署、账户或预订集成 | 无法把它当消费级在线服务使用 | 仅作为本地决策支持与可审计原型 |

## 报告问题

提交 issue 时请包含复现步骤、Python 版本、错误码和经过脱敏的最小输入。不要上传：

- API key、token、cookie、Authorization header；
- `runtime/` 原始目录；
- 本机绝对路径、账号 ID 或带个人资料的窗口截图；
- 尚未公开的真实出行日期、同行人信息、订单或预算明细。
